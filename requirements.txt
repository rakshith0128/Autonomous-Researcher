# Runtime dependencies for the container image.
#
# Kept in step with pyproject.toml by backend/tests/test_packaging.py, which
# fails CI if the two drift. The duplication buys Docker layer caching: this
# file changes rarely, so a code edit does not trigger a full dependency
# reinstall on every rebuild.

# --- web layer ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.3
python-multipart>=0.0.12

# --- contracts & config ---
pydantic>=2.9.0
pydantic-settings>=2.6.0

# --- orchestration ---
langgraph>=0.2.45
langchain-core>=0.3.15

# --- LLM access ---
openai>=1.54.0
tenacity>=9.0.0

# --- persistence & memory ---
aiosqlite>=0.20.0
chromadb>=0.5.20
fastembed>=0.4.2

# --- data acquisition ---
httpx>=0.27.2
beautifulsoup4>=4.12.3
trafilatura>=1.12.2
feedparser>=6.0.11

# --- messy data: PDFs, tables, images ---
pymupdf>=1.24.13
pdfplumber>=0.11.4
rapidocr-onnxruntime>=1.3.24
pillow>=11.0.0

# --- analysis ---
pandas>=2.2.3
numpy>=1.26.4,<2.1
scipy>=1.14.1
statsmodels>=0.14.4
scikit-learn>=1.5.2

# --- visualisation ---
plotly>=5.24.1
